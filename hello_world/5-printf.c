#include <stdio.h>

/**
 * main - holds code to be printed
 * Description: will print what's asked
 * Return: will return 0 if succesful
*/

int main(void)
{
	printf("with proper grammar, but the outcome if a piece of art,\n");
	return (0);
}
