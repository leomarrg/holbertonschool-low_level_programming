#include "main.h"
#include <stdio.h>

/**
 * mul - will multiply 2 integers
 * @a:one integer
 * @b: another integer
 * Return: will return the product of a and b
*/

int mul(int a, int b)
{
	return (a * b);
}
